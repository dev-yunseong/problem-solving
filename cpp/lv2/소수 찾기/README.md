# 소수 찾기

## 문제
- string이 주어질 때, 각 char의 조합으로 생성할 수 있는 숫자 중 소수의 개수를 반환하라

## 배운 점
```cpp
// set 합치기
set1.insert(set2.begin(), set2.end());
```
- vector는 인자로 전달 될 대 깊은 복사를 진행한다. 그렇기에 깊은 복사가 필요 없을 때는 참조로 전달한다.
- 객체를 함수 안에서 생성 후 함수가 종료 되면 객체의 메모리가 정리 되지만, 반환되면 소유권이 넘어간다.