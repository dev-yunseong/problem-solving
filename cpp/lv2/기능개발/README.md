# 기능 개발

## 문제 푼 방법
- 가장 앞의 기능이 개발까지 걸리는 시간을 계산하여서 그 시간만큼 전체적으로 progress를 반영
- 그 후 앞에서부터 100이 넘는 원소의 수를 계산하여서 저장

## 배운점
- list 자료구조는 index로 접근할 수 없다.
- list에서 iterator를 활용해서 순회할 수 있다. `list.begin()`, `list.end()`가 iterator를 반환하고 `iter++`를 통해서 다음 원소에 접근 가능하다. 또한 `*iter = 1`과 같이 값을 수정할 수 있다.
